version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm install -g @angular/cli@17
  pre_build:
    commands:
      - npm install
  build:
    commands:
      ng test --no-watch --no-progress --browsers=ChromeHeadlessCI #테스트 실행해주고 웹에서도 알려주는 도구?

#unit-test-buildspec 단계는 artifacts 설정 필요 없음